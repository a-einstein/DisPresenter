<UserControl 
    x:Class="RCS.DIS.Presenter.Views.VersieFilterAreaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300"
    >

    <StackPanel Grid.Row="0" Margin="{StaticResource BlockMargin}">
        <TextBlock Text="Versie" Style="{StaticResource TextBlockEmphasisStyle}"/>

        <!--Select the first element by IsSynchronizedWithCurrentItem. Currently there is even only one.-->
        <!--TODO Lost automatic selection again.-->
        <ComboBox Width="100" ItemsSource="{Binding Versies}" IsSynchronizedWithCurrentItem="True" SelectedValue="{Binding VersieSelected}"/>
    </StackPanel>
</UserControl>
